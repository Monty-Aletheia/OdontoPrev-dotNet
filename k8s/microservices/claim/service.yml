apiVersion: v1
kind: Service
metadata:
  name: claim-service
spec:
  selector:
    app: claim-service
  ports:
    - name: http
      protocol: TCP
      port: 8080           
      targetPort: 8080   
