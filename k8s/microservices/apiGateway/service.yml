apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
spec:
  type: LoadBalancer
  selector:
    app: api-gateway-service
  ports:
    - name: http
      protocol: TCP
      port: 5000           
      targetPort: 8080   
