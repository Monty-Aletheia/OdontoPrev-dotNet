apiVersion: v1
kind: Service
metadata:
  name: dentist-service
spec:
  selector:
    app: dentist-service
  ports:
    - name: http
      protocol: TCP
      port: 80           
      targetPort: 8080   
