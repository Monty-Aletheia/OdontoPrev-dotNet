apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: aletheiadotnet.azurecr.io/acr-aletheia-dotnet/auth-service:latest
          ports:
            - name: http
              containerPort: 8080
            - name: https
              containerPort: 8081
          envFrom:
            - configMapRef:
                name: bd-configmap
            - configMapRef:
                name: dentist-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      imagePullSecrets:
        - name: acr-secret
